package geeksForGeeks.Arrays;

// function 2 inputs clock
public class Adda247 {

    //User->userId,Name,emailId,phoneNo,parentUserId
    //Activity-> activityId,userId,createdOn and updatedOn
    //userId -1 xyz, xyz,0000, 0
    // 2,abc,abc,1111,1


    //select a.activityId,u.name from user u, activity a where u.userId==a.userId and u.phoneNo=99999999;
    // select u.userId,u1.parentUserId from user u,user u1 where u.userId!=u1.parentUserId and u1.parentUserId=1 and u.userId=1;
    //
}
